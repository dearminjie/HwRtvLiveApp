<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="160dp"
    android:layout_height="112dp"
    android:layout_gravity="center"
    android:background="@drawable/upload_bg">

    <ProgressBar
        android:id="@+id/upload_progress"
        style="@style/Widget.AppCompat.ProgressBar.Horizontal"
        android:layout_width="133dp"
        android:layout_height="5dp"
        android:layout_marginTop="40dp"
        android:max="100"
        android:progressDrawable="@drawable/log_progress_bar"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_upload_progress"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="13dp"
        android:textColor="#33ffffff"
        android:textSize="14sp"
        app:layout_constraintEnd_toEndOf="@id/upload_progress"
        app:layout_constraintStart_toStartOf="@id/upload_progress"
        app:layout_constraintTop_toBottomOf="@id/upload_progress"
        tools:text="uploading 20%"
        tools:visibility="gone" />

    <ImageView
        android:id="@+id/iv_upload_success"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginTop="22dp"
        android:src="@drawable/icon_success"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_upload_success"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="13dp"
        android:text="log upload success"
        android:textColor="#33ffffff"
        android:textSize="14sp"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/iv_upload_success" />

</androidx.constraintlayout.widget.ConstraintLayout>